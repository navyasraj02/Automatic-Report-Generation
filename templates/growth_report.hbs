<!-- filepath: f:\Automatic report generation\templates\growth_report.hbs -->

<html>
  <head>
    <meta charset="UTF-8" />
    <style>
      body { font-family: Arial, sans-serif; padding: 20px; color: #333; }
      .header { text-align: center; border-bottom: 3px solid #667eea;
      padding-bottom: 20px; margin-bottom: 30px; } h1 { color: #667eea;
      margin-bottom: 5px; } .meta-info { color: #666; font-size: 14px; }
      .section { margin-bottom: 30px; page-break-inside: avoid; } .section h2 {
      color: #764ba2; border-bottom: 2px solid #e0e0e0; padding-bottom: 10px;
      margin-bottom: 15px; } .growth-card { background: #f8f9fa; padding: 15px;
      margin-bottom: 15px; border-radius: 5px; border-left: 4px solid #667eea; }
      .growth-card h3 { color: #333; margin-bottom: 10px; } .comparison {
      display: flex; justify-content: space-between; margin-top: 10px; }
      .comparison div { flex: 1; padding: 10px; } .before { background: #fff3cd;
      margin-right: 10px; border-radius: 3px; } .after { background: #d4edda;
      border-radius: 3px; } table { width: 100%; border-collapse: collapse;
      margin-top: 15px; } th, td { padding: 12px; text-align: left;
      border-bottom: 1px solid #e0e0e0; } th { background-color: #f5f5f5;
      font-weight: 600; }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Growth Report</h1>
      <div class="meta-info">
        <p><strong>Experience:</strong> {{experience}}</p>
        <p><strong>Generated:</strong> {{generatedDate}}</p>
        <p><strong>Total Participants:</strong> {{totalParticipants}}</p>
      </div>
    </div>

    <div class="section">
      <h2>Growth Analysis</h2>
      {{#each exitForms}}
        <div class="growth-card">
          <h3>{{this.name}}</h3>
          <p><strong>Skills Gained:</strong> {{this.skillsGained}}</p>
          <p><strong>Achievements:</strong> {{this.achievements}}</p>
          <p><strong>Satisfaction Rating:</strong> {{this.satisfaction}}/10</p>
          {{#if this.feedback}}
            <p><strong>Feedback:</strong> {{this.feedback}}</p>
          {{/if}}
        </div>
      {{/each}}
    </div>

    <div class="section">
      <h2>Goal Achievement Summary</h2>
      <table>
        <thead>
          <tr>
            <th>Participant</th>
            <th>Goal</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          {{#each goals}}
            <tr>
              <td>{{this.name}}</td>
              <td>{{this.goal}}</td>
              <td>{{this.status}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </body>
</html>